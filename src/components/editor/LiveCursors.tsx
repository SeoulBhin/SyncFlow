// 실제 라이브 커서는 DocumentEditorPage의 CollaborationCursor TipTap 익스텐션이
// ProseMirror DOM 내부에 직접 렌더링합니다. 이 컴포넌트는 호환성을 위해 유지합니다.
export function LiveCursors() {
  return null
}
