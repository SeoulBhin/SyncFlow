# SyncFlow 프론트엔드 프로덕션 환경변수
# Vite가 `npm run build` 시 자동으로 이 파일을 읽습니다.
# 변경 시 반드시 재빌드(npm run build) → 서버 dist 폴더에 재배포 필요.

VITE_API_URL=https://syncflow.ai.kr
VITE_HOCUSPOCUS_URL=wss://syncflow.ai.kr/collab
